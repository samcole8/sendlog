files:
  - path: /var/auth.log
    type: MyType
    triggers:
      - name: MyTrigger
        formats:
          - name: MyFormat
            endpoints:
              - endpoint1
              - endpoint2
              - endpoint3
    enabled: true

plugins:
  - my_plugin_1
  - my_plugin_2
