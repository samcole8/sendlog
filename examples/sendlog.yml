files:
  - path: /var/auth.log
    type: my_type
    triggers:
      - name: MyTrigger
        transformations:
          - name: MyTransformation
            endpoints:
              - endpoint1
              - endpoint2
              - endpoint3
    enabled: true
