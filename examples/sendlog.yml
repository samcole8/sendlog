files:
  /var/auth.log:
    plugin: myplugin
    rules:
      MyRule:
        transformations:
          MyTransformation:
            endpoints:
              - myendpoint
              - myendpoint2
    enabled: true
